import '@testing-library/jest-dom';
// import ProduceList from '../exercise1/ProduceList';
// import { render, screen } from '@testing-library/react';

describe('ProduceList', () => {
  it('TEST NAME HERE', () => {
    // render(<ProduceList />)
    // expect
  });

  // add more tests
});
